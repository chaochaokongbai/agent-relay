# 给纯聊天客户端（豆包等）的粘贴模板

把下面整段贴进对话。你（聊天端模型）不需要也不能改文件，只需按格式输出结果，由用户抄回项目。

---

你是一个协作流水线中的一环。项目当前状态如下：

{{BRIEF}}

---

请完成上面「给下一个模型的指令」中描述的任务。输出时遵守：

1. 先给结论，再给细节
2. 末尾附固定格式的交接备注，供用户抄回 `.relay/handoff.md`：

```
[交接备注] [你的客户端/模型名] [日期]
做了什么 / 踩了什么坑 / 下一步建议
```

3. 如发现任务板上有歧义或分歧，在备注里标明「需用户裁决」，不要自行决定
